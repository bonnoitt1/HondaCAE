<% if @log_items_weights.any? %>
 <table border="2">
 <tr>
   <th bgcolor="#2b2b2c"><font color="#ffffff"><h3>Entriess</h3></font></th>
 </tr>
  <tr class="goals">
   <%= render partial: 'shared/log_item_weight', collection: @log_items_weights %>
  </tr>
 </table>
  <%= will_paginate @log_items_weights %>
 
<% end %>

<div class="row">
          <div class="col-lg-12">
            <div class="page-header">
              <h1 id="tables">Past Weight Entries..</h1>
            </div>
<div class="bs-example table-responsive">
		<% if @log_items_weights.any? %>
              <table class="table table-striped table-bordered table-hover ">
                <thead>
                  <tr>
                    <th><font color="#ffffff">Weight</font></th>
                    <th><font color="#ffffff">Posted at</th></font>
                    <th><font color="#ffffff">Delete</th></font>
                  </tr>
                </thead>
                <tbody>
                  <tr class="weights">
                    <%= render partial: 'shared/log_item_goal', collection: @log_items_goals %>
                  </tr>
                  
                </tbody>
              </table>
		<%= will_paginate @log_items_goals %>
            </div><!-- /example -->
  </div>
 </div>

<% end %>
