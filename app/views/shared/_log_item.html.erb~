<tr id="<%= log_item.id %>">
<td>
  <span class="content"><%= log_item.content %></span>
  <span class="timestamp">
   Posted <%= (log_item.created_at) %> ago.
  </span>
 <% if current_user?(log_item.user) %>
   <%= link_to "delete", log_item, method: :delete,
                                   data: { confirm: "Are you sure?" },
                                   title: log_item.content %>
 <% end %>
</td>
</tr>
